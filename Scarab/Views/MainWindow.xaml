<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="670" Height="450"
        x:Class="Scarab.Views.MainWindow"
        Icon="/Assets/ConstructionKnight.ico"
        Title="Scarab+"
		xmlns:vm="using:Scarab.ViewModels"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaTitleBarHeightHint="-1"
        x:DataType="vm:MainWindowViewModel"
        TransparencyLevelHint="Transparent"
        Background="Transparent">
	<Border CornerRadius="{CompiledBinding CornerRadius}" BorderBrush="{CompiledBinding BorderBrush}" BorderThickness="{CompiledBinding BorderThickness}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="30" />
				<RowDefinition Height="1" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Rectangle Grid.RowSpan="3" Fill="#282828" IsHitTestVisible="False" />
			<Image Source="/Assets/ConstructionKnight.ico" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5,0,0,1"/>
			<TextBlock Height="30" FontSize="23" Text="{CompiledBinding AppName}" VerticalAlignment="Center" Margin="40,0,0,5" />
			<Rectangle Grid.Row="1" Fill="#a9a9a9"/>
			<UserControl Content="{Binding Content}" Grid.Row="2"/>
		</Grid>
	</Border>
</Window>
